---
import ComparisonPage from '../components/site/ComparisonPage.astro'
import Layout from '../layouts/Layout.astro'
---

<Layout>
  <ComparisonPage kicker="Alternatives" title="Comparing route isolation instead of products" summary="The question in this repo is whether Starlight CSS stays scoped to docs routes. This page exists to broaden the website route surface without pulling docs components into the website tree." />
</Layout>
