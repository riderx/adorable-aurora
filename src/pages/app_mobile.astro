---
import MarketingPage from '../components/site/MarketingPage.astro'
import Layout from '../layouts/Layout.astro'
---

<Layout>
  <MarketingPage kicker="App Mobile" title="A marketing page with landing-style gradients and cards" summary="The real site has many flat pages that share the same layout shell and many of the same visual primitives. The repro now does too." primaryLabel="Inspect build" primaryHref="/docs/" secondaryLabel="Back home" secondaryHref="/" />
</Layout>
