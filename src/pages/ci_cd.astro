---
import Layout from '../layouts/Layout.astro'
import MarketingPage from '../components/site/MarketingPage.astro'
---

<Layout>
  <MarketingPage kicker="CI/CD" title="Preview mode should not inject docs reset onto website pages" summary="The original bug shows up in preview/build output, so this repro keeps growing the generated route and CSS graph until the same manifest break appears." primaryLabel="View docs" primaryHref="/docs/" secondaryLabel="See pricing" secondaryHref="/pricing/" />
</Layout>
