---
import Layout from '../layouts/Layout.astro'
import MarketingPage from '../components/site/MarketingPage.astro'
---

<Layout>
  <MarketingPage kicker="Consulting" title="Flat routes, shared layout, isolated docs CSS" summary="That is the invariant the real app breaks today. This reproduction is now aimed directly at that invariant instead of staying in a too-clean happy path." />
</Layout>
