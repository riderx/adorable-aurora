---
import Layout from '../layouts/Layout.astro'
import MarketingPage from '../components/site/MarketingPage.astro'
---

<Layout>
  <MarketingPage kicker="Sponsor" title="Marketing routes should share website CSS and nothing else" summary="The repro still uses one docs app under `/docs/`, but the rest of the site now looks more like a real marketing surface from Astro's perspective." />
</Layout>
