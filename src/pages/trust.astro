---
import Layout from '../layouts/Layout.astro'
import TrustPage from '../components/site/TrustPage.astro'
---

<Layout>
  <TrustPage kicker="Trust" title="Closer to landing, still much smaller" summary="This is the point of the reproduction: keep the app small enough to debug, but large enough that Astro and Starlight behave the same way they do in production." />
</Layout>
